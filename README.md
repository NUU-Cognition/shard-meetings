# Meetings

Meetings shard

## Structure

```
Shards/(Dev) Meetings/
  shard.yaml              # Manifest
  init-m.md    # Init file — shard context
  skills/                 # sk-m-{name}.md
  workflows/              # wkfl-m-{name}.md
  templates/              # tmp-m-{name}.md
  knowledge/              # knw-m-{name}.md
  assets/                 # ast-m-{name}.{ext}
  install/                # Files to install into Mesh/
```
